# 动漫数据分析与推荐系统 Python 算法引擎依赖
# 安装：pip install -r requirements.txt
# （后续 prediction/recommendation 引入建模/推荐依赖时在此追加）

pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.13.0

# --- 建模/推荐依赖（algorithm/recommendation 与 algorithm/prediction 的 API 脚本） ---
scikit-learn>=1.5.0
scikit-surprise>=1.1.4
lightgbm>=4.3.0
joblib>=1.3.0
scipy>=1.11.0
pymysql>=1.1.0
wordcloud>=1.9.0

# --- 深度学习依赖（NCF 神经协同过滤）---
# CPU 版安装（体积小，无需 GPU）：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
